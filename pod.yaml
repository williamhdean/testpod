 apiVersion: v1
 kind: Pod
 metadata:
   name: nginx-testpod
   labels:
     app: testpod
 spec:
   containers:
     - name: nginx
       image: nginx
       ports:
         - containerPort: 80
           name: "nginx-http"
